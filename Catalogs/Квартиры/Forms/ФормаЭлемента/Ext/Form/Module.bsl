
&НаКлиенте
Процедура ПередаватьДанныеПриИзменении(Элемент)
	Если Объект.ПередаватьДанные = Истина Тогда
	
		 Элементы.Обменник.Видимость = Истина;
	Иначе
		 Элементы.Обменник.Видимость = Ложь;
	КонецЕсли;
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Объект.ПередаватьДанные = Истина Тогда
		 Элементы.Обменник.Видимость = Истина;
    Иначе
		 Элементы.Обменник.Видимость = Ложь;
	КонецЕсли;
КонецПроцедуры
